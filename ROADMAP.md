# 🗺 План развития «Крым.Гид»

> Создан 16.09.2026. Текущая версия — **1.12.0**.
> Принципы из [docs/architecture.md](docs/architecture.md) сохраняются:
> без БД, без API-ключей, деградация вместо падения, фронт без сборки.
>
> **План выполнен**: фазы 0.11.0 → 1.9.0 закрыты, бэклог разобран
> (последним — «Хвосты» в 1.9.0). Дальше — [идеи без привязки к версии](#бэклог-без-привязки-к-версии):
> они ждут повода (спрос, живой сервер, новый язык), а не очереди.
> 1.10.0 «Сезон и вывеска» выросла не из плана, а из аудита контента:
> данные сверили сами с собой и нашли места, где они спорят друг с другом.
> 1.11.0 «Цена вопроса» — тот же аудит, третий хвост: вопрос квиза
> о бюджете не влиял на подбор, потому что уровни стояли на глаз.
> 1.12.0 «Описания и подписи» — тоже аудит, но молчавший: поле
> `description` у места требовалось, но ничего у него не требовало,
> а подписи источников годами лежали комментарием в стол.

## Легенда статусов

`[x]` — готово · `[~]` — в работе · `[ ]` — запланировано

---

## Фаза 0.11.0 — «Качество и контракт API» `[x]`

Цель: жёсткий API-контракт, автоматический контроль стиля и версий,
первые JS-тесты. Без изменения пользовательского поведения.

| # | Задача | Статус |
|---|---|---|
| A1 | Валидация `QuizIn` через `Literal` → `422` на мусор вместо молчаливых дефолтов | `[x]` |
| A2 | `GET /api/weather?city=<неизвестно>` → `404` вместо «все города» | `[x]` |
| A3 | Тест согласованности версий: `config.py` == `sw.js` == `README.md` == `docs/api.md` | `[x]` |
| A4 | Ruff `check` в CI, `line-length = 88` по [dev-гиду](docs/development.md) | `[x]` |
| A5 | JS-тесты шаринга плана: вынести `buildPlanString`/`parsePlanParam` в `static/plan-link.js`, тесты на `node:test` без зависимостей + шаг в CI | `[x]` |
| A6 | `CHANGELOG.md`, бамп `0.11.0`, доки (`api.md`: `422`/`404`) | `[x]` |

## Фаза 0.12.0 — «Фронт и доступность» `[x]`

Цель: доступность, шаринг состояния, безопасность заголовков.

| # | Задача | Статус |
|---|---|---|
| B1 | a11y: осмысленные `alt` у изображений карточек, `focus trap` в модалке, `aria-live` у тостов, skip-link «к содержимому» | `[x]` |
| B2 | Фильтры каталога в URL (`#/catalog?tag=&area=&q=&sort=`) — шаринг подборок: `static/catalog-link.js` + 8 JS-тестов, кнопка «🔗 Ссылка на подборку» | `[x]` |
| B3 | Open Graph / Twitter Card мета-теги для шаринга ссылок (origin из `SITE_ORIGIN`/заголовков в `GET /`) | `[x]` |
| B4 | `Content-Security-Policy`: inline-обработчики убраны, PWA и Яндекс-ссылки работают; `frame-ancestors` настраивается env | `[x]` |
| B5 | Аудит веса картинок (`static/img` 3.1 МБ → 0.75 МБ): ресайз/качество через `tools/optimize_images.py`, `hero.jpg` в `fetchpriority="high"`, бюджет закреплён тестами | `[x]` |
| B6 | Тесты и доки: `tests/test_assets.py`, CSP/OG-тесты в `test_api.py`, CHANGELOG, версия 0.12.0 | `[x]` |

## Фаза 1.0 — «Контент и релиз» `[x]`

Цель: полнота данных и готовность к продакшену.

- [x] Аудит покрытия тегов: `TAGS` (15) vs опции квиза (15) vs `PROFILES` (15)
      vs `PURPOSE_TOPICS` (15) — все теги доступны в квизе, имеют профиль и
      маппинг на темы новостей; покрытие закреплено тестами.
- [x] Расширение каталога: 52 → **60 точек**, Западный Крым 8 → **16**
      (Сасык-Сиваш, Бакальская коса, Донузлав, Беляус, Межводное, кенасы,
      Окунёвка, «Крым в миниатюре»); поле `hours` у 13 объектов со
      стабильным расписанием. Минимум 10 мест на район — в тестах.
- [x] Деплой-документация: [docs/deploy.md](docs/deploy.md),
      `docker-compose.yml` (app + Caddy, HTTPS по ACME), `.env.example`,
      backup `cache/` — `deploy/backup-cache.sh` и `restore-cache.sh`;
      прод-конфигурация закреплена тестами и шагом CI.
- [x] E2E smoke ключевых сценариев (квиз → план → ссылка) —
      `tests/test_e2e_smoke.py`, 13 тестов. Playwright сознательно не взяли:
      он тянет npm и браузерные бинарники против принципа «фронт без
      сборки, тесты без сети». Сценарии закрыты на уровне HTTP плюс
      сверка Python-двойника ссылки с настоящим `plan-link.js` в Node.

## Фаза 1.1 — «После релиза» `[x]`

Цель: то, что стало видно на полном каталоге и живом деплое.

- [x] Ленивый рендер каталога: первая порция 12 карточек вместо 60,
      догрузка по прокрутке (`IntersectionObserver`) и кнопкой
      «Показать ещё» — `static/catalog-page.js`, 7 JS-тестов.
      Попутно починена повторная привязка обработчиков в `bindCards`.
- [x] `hours` для оставшихся объектов с расписанием: Мангуп-Кале,
      Эски-Кермен, Неаполь Скифский, Судакская крепость, Калос-Лимен,
      аквариум Алушты (13 → 19).
- [x] Фильтр «открыто сейчас» поверх `hours`: вместо парсинга свободного
      текста завели парное машинное поле `schedule` — текст остаётся
      точным для человека, структура фильтруется машиной, расхождение
      ловится тестом. `static/open-now.js`, 11 JS-тестов.
- [x] Автодеплой из CI по тегу — перенесённый в бэклог пункт сделан
      в фазе 1.6.0 (`G5`: `.github/workflows/deploy.yml` + `deploy/deploy.sh`),
      отметка здесь просто не была обновлена. В 1.9.0 сценарий дополнен
      репетицией и проверкой версии до сборки.

> Точность «открыто сейчас» доведена до дня в 1.2 (`firstDay`/`lastDay`);
> в тексте `hours` остались только то, что закодировать нельзя —
> санитарные дни и закрытия на непогоду. Об этом интерфейс честно
> предупреждает: «уточняйте на месте».

## Фаза 1.2.0 — «Точность данных и шаринг избранного» `[x]`

Цель: убрать известную неточность «открыто сейчас» (огрубление до
месяца, оговорённую в 1.1) и сделать избранное шарящимся — как
подборка каталога.

| # | Задача | Статус |
|---|---|---|
| C1 | Дневная точность `schedule`: `firstDay`/`lastDay` в правилах `open-now.js`; точные границы у Ливадии, Массандры, Воронцовского дворца; подпись «открывается 16 июня» вне сезона; двусторонняя сверка границ с текстом `hours` тестом | `[x]` |
| C2 | Шаринг избранного: `static/favs-link.js` (UMD) + 9 JS-тестов; кнопка «🔗 Ссылка на избранное»; импорт по `#/favs?f=…` (только id из каталога получателя, только добавление) | `[x]` |
| C3 | `ruff format` для всего репо отдельным коммитом (17 файлов) + `format --check` в CI; версия ruff закреплена в `requirements-dev.txt` | `[x]` |

## Фаза 1.3.0 — «Купальня» `[x]`

Цель: превратить погодные данные в практический ответ «можно ли купаться
сегодня» — и не соврать, если модель молчит про точку.

| # | Задача | Статус |
|---|---|---|
| D1 | `app/services/marine.py`: Open-Meteo Marine (температура поверхности воды + волна) по 15 приморским курортам, кэш `MARINE_TTL` (3 часа: волновую модель обновляют раз в 12 ч, воду — раз в сутки), деградация до `available: false` вместо падения | `[x]` |
| D2 | Купальный индекс: `verdict()` с приоритетом безопасности — волна ≥1,5 м «опасно», ≥1,0 м «не стоит», дальше вода 23/20/16 °C → «комфортно / можно / прохладно / холодно»; вердикт считается по «сырым» значениям (22.6 °C показывается как +23°, но остаётся «можно») | `[x]` |
| D3 | `GET /api/sea?city=&refresh=1`: точки, подписи волнения словами, сводка `warmest`; честный 404 для материковых курортов (Симферополь, Бахчисарай, Белогорск — моря нет) | `[x]` |
| D4 | Блок «🌊 Море и купание» на главной: карточки курортов, цветной вердикт, дисклеймер «у берега вода может быть прохладнее» | `[x]` |
| D5 | `tests/test_sea.py` (54 теста) + API-, конфиг- и ассет-тесты; согласованность `SEA_POINTS` ↔ `CITIES` (id, имена, «точка в море и рядом со своим городом») держат тесты | `[x]` |

> Морская модель считает **открытую воду**: точки вынесены на 1–8 км от
> берега, а у самого берега и на мелководье вода бывает другой. Об этом
> интерфейс предупреждает прямо в блоке.

## Фаза 1.4.0 — «По часам» `[x]`

Цель: показать прогноз там, где он решает — «выйдем через час или
сейчас» — и не соврать про «сейчас» на данных из кэша.

| # | Задача | Статус |
|---|---|---|
| E1 | `hourly` в ответе погоды: температура, WMO-код и вероятность осадков на 24 часа вперёд; окно начинается с текущего часа города (сравнение ISO-строк от `current.time`, без конвертации в пояс клиента) | `[x]` |
| E2 | `static/hourly.js` (UMD, 15 JS-тестов): окно показывает клиент по **крымскому** времени — ответ кэшируется на час, и «сейчас» в нём к концу часа устаревает; полностью устаревшее окно даёт пустой список, а не вчерашние часы | `[x]` |
| E3 | Карточка города: раскрывающийся блок «По часам» на нативном `<details>` (CSP-совместимо, работает с клавиатуры и скринридером), 12 часов, подпись «сейчас», «💧%» от 30% | `[x]` |
| E4 | Деградация: пропущенные значения модели → `null` и эмодзи-заглушка; блок прячется, если часов нет вовсе | `[x]` |
| E5 | Тесты: `test_weather.py` (окно, лимит, мусор, короткие массивы), API-тест на `hourly`, четыре ассет-теста (UMD + проводка, крымское время, нативный `<details>`, порог осадков в модуле) | `[x]` |

> В `hourly[]` — локальные времена курорта, а не клиента: сервер их
> только нарезает, окно отсчитывает фронт. Так «сейчас» остаётся верным
> и через 59 минут после обновления кэша.

## Фаза 1.5.0 — «Карта» `[x]`

Цель: опциональный интерактивный слой поверх офлайн SVG-схемы — по
желанию пользователь может приблизить и покрутить карту с тайлами
OpenStreetMap, не теряя привычного вида при открытии по умолчанию
(и без потери офлайн-работоспособности).

| # | Задача | Статус |
|---|---|---|
| F1 | Leaflet 1.9.4 локально в `static/vendor/` (CSS+JS+изображения), без CDN, без npm-сборки; правка путей к иконкам под `/static/vendor/images/` | `[x]` |
| F2 | `static/leaflet-map.js` (UMD): ленивая загрузка Leaflet при первом включении, кастомные цветные divIcon-маркеры в стиле SVG-схемы, линия плана + нумерованные остановки | `[x]` |
| F3 | Тулбар карты: переключатель «📐 Схема / 🛰 Спутник/карта» с подписями о доступности; дефолт — офлайн-схема | `[x]` |
| F4 | Деградация: неудачная загрузка Leaflet/тайлов → тост и автоматический откат на SVG, не показываем пустой квадрат | `[x]` |
| F5 | CSP: `img-src`/`connect-src` разрешить только `https://tile.openstreetmap.org`, `script-src 'self'` не ослаблять; vendor в precache SW | `[x]` |
| F6 | Тесты: 5 JS-тестов модуля, 8 новых ассет-тестов, версия 1.5.0 везде | `[x]` |

> Leaflet не добавляется в холодную загрузку: модуль подгружает ~160 Кб
> скрипта и ~15 Кб стилей только в момент, когда пользователь нажал
> «Спутник/карта». При неудаче откат мгновенный.

## Фаза 1.6.0 — «Щит» `[x]`

Цель: закрыть дорогие ручки от случайных петель и слишком усердных
клиентов (без БД и внешних сервисов) и перестать выкатывать релизы
руками — тег должен ехать на сервер сам.

| # | Задача | Статус |
|---|---|---|
| G1 | `app/ratelimit.py`: скользящее окно (журнал времён запросов) на клиента, `Decision` с `remaining`/`retry_after`, вытеснение давних ключей (`RATE_LIMIT_MAX_KEYS`) | `[x]` |
| G2 | Middleware `rate_limit` в `main.py`: лимит только на `POST /api/quiz/evaluate` и `?refresh=1` у ленты/погоды/моря; `429` + `Retry-After` и `X-RateLimit-*`; `security_headers` стал внешним слоем, поэтому CSP остаётся и на отказе | `[x]` |
| G3 | `TRUST_PROXY`: за своим Caddy клиента определяет `X-Forwarded-For` (compose передаёт `1`), иначе адрес соединения — чужому клиенту заголовок верить нельзя | `[x]` |
| G4 | Фронт: `static/rate-limit.js` (UMD, 10 JS-тестов) — `Retry-After` → «попробуйте через 5 минут»; квиз и «Обновить» новостей больше не показывают сырой статус, кнопка после 429 снова рабочая | `[x]` |
| G5 | Автодеплой по тегу: `.github/workflows/deploy.yml` (preflight-тесты → SSH) + `deploy/deploy.sh` (git checkout тега → `compose up -d --build` → ожидание healthy → сверка версии из `/api/health`); откат — тем же workflow с прошлым ref | `[x]` |
| G6 | Тесты: `tests/test_ratelimit.py` (35), конфиг- и ассет-тесты лимитов и деплоя, `rate-limit.js` в CI-проверке синтаксиса; версия 1.6.0 везде | `[x]` |

> Лимит — не защита от DDoS: счётчики в памяти одного процесса и
> обнуляются рестартом. Его задача — остановить случайную петлю
> в браузере и скрипт-переборщик квиза, а не тяжёлый трафик: от него
> спасает прокси (Caddy), как и раньше.

## Фаза 1.7.0 — «Свои бюджеты» `[x]`

Цель: перестать мерить ленту, погоду и море одним числом — у каждой
ручки свой бюджет — и сделать так, чтобы этот бюджет было по чему
настраивать (без БД и внешних метрик), а фронт не тратил его вслепую.

| # | Задача | Статус |
|---|---|---|
| H1 | Раздельные бюджеты: `RATE_LIMIT_NEWS` / `_WEATHER` / `_SEA` (+`_WINDOW`) вместо общего `RATE_LIMIT_REFRESH`; числа привязаны к тому, как часто данные меняются (лента — 6 за 5 мин, погода — 4 за 15 мин, море — 3 за 30 мин); старая переменная осталась фолбэком для трёх ручек | `[x]` |
| H2 | `Rule` в `ratelimit.py`: правила как данные, `rules()` читает конфиг на момент вызова, `match_rule()` возвращает правило, короткое имя правила — в ключе счётчика | `[x]` |
| H3 | Наблюдаемость: счётчики «пущено/отбито» по правилам в лимитере, строка в лог на каждый `429`, бесплатная ручка `GET /api/limits` с бюджетами и нагрузкой процесса | `[x]` |
| H4 | `X-RateLimit-Rule`: бюджетов стало несколько, и клиенту нужно знать, чей остаток он прочитал | `[x]` |
| H5 | Фронт: `readBudget`/`cooldownSeconds`/`budgetText` в `static/rate-limit.js` (+11 JS-тестов); «осталось 2 обновления из 6» рядом с кнопкой, кнопка гаснет до открытия окна вместо повторного `429` (и в ленте, и в квизе) | `[x]` |
| H6 | Тесты и доки: `test_ratelimit.py` (+15), `test_config.py` (дефолты, env, фолбэк), ассет-тесты проводки и `.env.example`, таблица бюджетов в [docs/api.md](docs/api.md#ограничение-частоты-запросов), CHANGELOG, версия 1.7.0 | `[x]` |

> Бюджет — не украшение отчёта: он виден и в `GET /api/limits`, и строкой
> в логе на каждый отказ. Именно эти два источника и заменяют метрики —
> раздельные окна подкручивают по факту, а не по интуиции.

> Сделано в 1.7.0 — PR [#12](https://github.com/dmitrymixx1-dot/crimea_gid/pull/12).

## Фаза 1.8.0 — «Вторая речь» `[x]`

Цель: ключевые экраны понятны англичанину — без БД, без API-ключей, без
машинного перевода и без единой правки в серверном коде: English-пакет
лежит статикой, русская речь от локализации не зависит вовсе.

| # | Задача | Статус |
|---|---|---|
| I1 | `static/i18n.js` (UMD): ключ — русская фраза, `normalize` + сворачивание чисел в `{n}`, `params`, обход DOM (`title`/`aria-label`/`placeholder`/`alt`, `data-i18n="skip"`), `applyLabels`/`stripLabels`, `pickLang`, `applyDoc` | `[x]` |
| I2 | `static/i18n/en.json`: `ui` (строки каркаса и `t()`-шаблоны, сверка двусторонняя), `labels` (подписи данных, сервисов и модулей), `places` (60 мест по id, поля — те же, что в данных) | `[x]` |
| I3 | Проводка: `#lang-toggle` в шапке, подключение модуля до `app.js`, `state.lang`/`state.labels`, `?lang=` + `localStorage["crimea_lang"]`, перерисовка маршрута, `i18n.js` в SHELL (сам пакет — нет) | `[x]` |
| I4 | Фронт переведён там, где DOM-обход бессилен: тосты, `aria-live`, счётчик каталога, подписи лимита и countdown, `alt` картинок, `aria-label` маркеров, легенда и подсказки карты, чипы причин (`tReason`), формы чисел (`nform`), локаль дат и времени | `[x]` |
| I5 | `tools/i18n_extract.py` — покрытие локали как тест: разбирает разметку и `app.js` как браузер, добавляет серверные подписи, печатает дыры, мёртвые ключи и фразы, которые обязаны стать `t()`-шаблоном; `tools/i18n_en/` — переводы как данные + `build.py --check` | `[x]` |
| I6 | Тесты и доки: `tests/js/i18n.test.js` (25), `tests/js/i18n-labels.test.js` (11), `tests/test_i18n.py` (14), ассет-тесты проводки, рецепт перевода в [development.md](docs/development.md#как-расширять), поток локализации и таблица кэшей в [architecture.md](docs/architecture.md), контракт бандла в [api.md](docs/api.md#get-statici18nenjson), CHANGELOG, версия 1.8.0 | `[x]` |

> Дыра в локали невидима: экран выглядит переведённым наполовину, и никто
> не замечает. Поэтому покрытие — тест, а не надежда: поменяли русскую
> строку в разметке, не тронув словарь, и `test_i18n.py` покажет ключ.

> Не делаем: машинный перевод, заголовки новостей (чужие тексты), названия
> районов в `area` (участвуют в фильтрах и в ссылке подборки). Третья речь =
> ещё один `static/i18n/<lang>.json` плюс пункт в переключателе.

## Фаза 1.9.0 — «Хвосты» `[x]`

Цель: разобрать бэклог — то, что откладывалось «до повода», — и довести
дорожную карту до конца. Три хвоста: общий бюджет на клиента, англоязычная
лента и проверка выката без живого сервера.

| # | Задача | Статус |
|---|---|---|
| J1 | Общий потолок клиента поверх раздельных бюджетов: `Budget` + `RateLimiter.reserve()` (один запрос — два счётчика, проходит только если место есть в обоих), `total_rule()`/`check_rule()`/`Verdict.binding`, `RATE_LIMIT_TOTAL` (120) и `_WINDOW` (600 с), `0` — выключить | `[x]` |
| J2 | Честность отказа: потолок не тратит слот ручки (данных клиент не получил), `retry_after` только у связавшего бюджета и покрывает самое долгое ожидание, основные заголовки описывают связавший бюджет (`X-RateLimit-Rule: total`), сам потолок — в `X-RateLimit-Total-*`, строка в логе на каждый отказ | `[x]` |
| J3 | Наблюдаемость и фронт: `total`/`clients`/`counters` в `GET /api/limits`, `stats` не пишет чужой отказ в чужой счётчик; `readCeiling()` в `static/rate-limit.js` (+6 JS-тестов), кнопка гаснет по потолку, а подпись меняется на «обновления исчерпаны» вместо обещания «осталось 4 из 6» | `[x]` |
| J4 | «Дельфин»: вывески изданий стали частью контракта локали (`source_labels()` в `tools/i18n_extract.py` — бейдж новости печатает имя из данных, статика его не видит), добавлены `РБК`, `РБК · TG`, `Крым.Цифровой`; оговорка «Заголовки остаются русскими…» — только на английском экране; чужие тексты по-прежнему не переводятся | `[x]` |
| J5 | Репетиция выката вместо ожидания живого сервера: `deploy/deploy.sh --dry-run` (ref, состав репозитория, `bash -n` всех скриптов, `APP_VERSION` == имени тега, `.env`/`DOMAIN`) печатает план и ничего не меняет; шаг в CI на каждый push, шаг до SSH в `deploy.yml` и ручной `dry_run=true` (секреты — предупреждением); сверка версии перенесена до сборки; префлайт деплоя гоняет то же, что CI (`ruff format --check`, `build.py --check`) | `[x]` |
| J6 | Тесты и доки: `test_ratelimit.py` (47 → 68), `test_assets.py` (57 → 68, включая настоящие запуски `--dry-run`), `test_config.py`, `test_i18n.py`, `tests/js/rate-limit.test.js` (21 → 27); таблицы лимитов и заголовков в [api.md](docs/api.md), раздел «Репетиция выката» в [deploy.md](docs/deploy.md), CI и отладка в [development.md](docs/development.md), README, CHANGELOG, версия 1.9.0 | `[x]` |

> Потолок — не вторая защита от DDoS, а защита от «по чуть-чуть»: раздельные
> окна защищают ручку, но не процесс. Числа выбраны так, чтобы резать
> размазанную нагрузку (120 за 10 минут против ~320, которые позволили бы
> бюджеты ручек) и не задевать живого человека (единицы дорогих запросов
> за сессию). Проверить, что потолок теснее суммы ручек, — тест,
> а не договорённость (`test_config.py`).

> Живой сервер репетиция не заменяет: она проверяет сценарий, состав
> и версии, но не SSH-подключение и не поведение Docker на боевой машине.
> Первый настоящий выкат — за оператором, с логом workflow до конца;
> всё, что можно проверить без сервера, проверяется теперь на каждый push.

## Фаза 1.10.0 — «Сезон и вывеска» `[x]`

Цель: контент, который не спорит сам с собой. Аудит данных нашёл три
расхождения — `season` против `hours`, границы координат в доках против
каталога, вывеска в подвале против `sources.json`. Ни одной новой сущности
не выдумано: каждая правка выведена из того, что уже лежало в репозитории,
и закрыта тестом, чтобы не разъехалась обратно.

| # | Задача | Статус |
|---|---|---|
| K1 | Сезон выводится из расписания: 15 мест из 19 публикующих часы получили зиму (покрытие 8 → **23** места, Восточный Крым перестал быть пустым), Судакская крепость и алуштинский аквариум — ещё и весну (41 → **43**); до этого квиз штрафовал их на −1.0 балла за сезон, в который они открыты по нашим же данным | `[x]` |
| K2 | Правило закреплено двусторонним тестом (`test_season_agrees_with_published_schedule`): расписание работает в сезон → сезон в списке, и наоборот. Места без `schedule` не тронуты — сверять их не с чем, а выдумывать «работает зимой» нельзя | `[x]` |
| K3 | Квиз не отвечает пустотой: на каждый сезон ≥20 мест и хотя бы одно в каждом из четырёх районов (`test_every_season_and_area_has_something_to_offer`) | `[x]` |
| K4 | Границы координат в `docs/data.md` приведены к каталогу (заявленные 33.0–37.0 в. д. не включали Тарханкут и ещё 7 точек Запада, 44.0–45.6 с. ш. — Бакальскую косу) и сверяются тестом, который читает документ, а не держит копию чисел | `[x]` |
| K5 | Вывеска в подвале: «7 RSS-лент и 4 телеграм-канала» вместо пяти названных лент из одиннадцати, «Погода и море: Open-Meteo» (Marine считал воду и волну, но упомянут не был), «Карта: OpenStreetMap» (авторство тайлов живёт и там, где слой Leaflet выключен); числа берутся из `sources.json` и сверены тестом вместе с английской строкой | `[x]` |
| K6 | Локаль и версия: новая вывеска в `tools/i18n_en/tr_ui.py`, пересборка `static/i18n/en.json` (покрытие полное — 392 строки, 0 дыр), `CHANGELOG.md`, версия 1.10.0 в `config.py`, README, `docs/api.md` и кэше PWA | `[x]` |

> Проверить, что данные не спорят друг с другом, дешевле, чем добавить
> ещё десять мест: расхождение невидимо для редактора (карточка выглядит
> правильно), зато его видно пользователю — зимний квиз выдавал 8 мест
> на весь полуостров и ни одного на востоке.

## Исправления 1.10.1 — «Проверка в браузере» `[x]`

- `[x]` Квиз действительно проходит все 7 вопросов, а не отправляет
  единственный `purpose` и получает серверные значения по умолчанию.
- `[x]` Мобильное меню доступно: базовое `display: none` больше не
  перекрывает правило для узкого экрана.
- `[x]` Leaflet включается без ошибки `getBounds is not a function`;
  очистка карты при смене экрана/фильтра и обработка ошибок перерисовки.
- `[x]` Совпавшие координаты разведены в обоих слоях карты, с линиями
  к исходной точке и учётом соседних маркеров. Координаты не изменены.
- `[x]` Регрессии переходов квиза и раскладки в JS-тестах; опциональный
  браузерный smoke (`tools/browser_smoke.py`) на 1280 и 390 px.

Следующий этап — настоящий деплой и проверка внешних сервисов на домене.
Браузерная проверка с подменёнными тайлами доступность OSM не доказывает.

## Фаза 1.11.0 — «Цена вопроса» `[x]`

Цель: чтобы ответ о бюджете что-то значил. Аудит 1.10.0 нашёл последний
пункт из трёх: `budget=3` стоял у одного места из 60, поэтому «комфорт»
и «премиум» в квизе давали одну и ту же подборку. Уровни вывели из цен,
которые уже лежали в `price_hint`, и добавили машинную цену входа —
ровно так же, как 1.10.0 вывела сезон из расписания.

| # | Задача | Статус |
|---|---|---|
| L1 | Машинная цена рядом с человеческой подсказкой: `price` — минимум, за который место доступно (0 — бесплатно); `budget` пересчитан как полоса **самой дорогой** цены из `price_hint` (≤1 000 ₽ → 1, ≤3 000 ₽ → 2, дороже → 3). 22 места сменили уровень, шкала перестала быть вырожденной: 47 / 9 / 4 | `[x]` |
| L2 | Правило закреплено двусторонними тестами (`test_price_is_the_lowest_price_of_the_place`, `test_budget_level_follows_the_highest_price`): тест читает подсказку и требует совпадения чисел, а `test_budget_levels_use_the_whole_scale` — что у каждого уровня есть адресаты и потолок эконома что-то отсекает | `[x]` |
| L3 | Пороги — одни на квиз и матч-мейкер: `BUDGET_CEILING` в `recommend.py`, текст подсказок сверяет `test_budget_thresholds_match_the_quiz` («до 1 000 ₽», «1 000–3 000 ₽», у премиума чисел нет) | `[x]` |
| L4 | Баллы за бюджет стали честными и асимметричными: эконом платит за **вход** (`price` выше 1 000 ₽ → −2.0 и причина «Дороже, чем ваш бюджет»), комфорт — до 3 000 ₽, премиум входа не ограничивает и **предпочитает** места с дорогими впечатлениями (`budget` = 3 → +1.0 и чип «Премиум-впечатление»). Раньше эконом получал минус за место с бесплатным входом только потому, что рядом продают дайвинг | `[x]` |
| L5 | Ответы больше не пустышка: тесты требуют, чтобы каждый ответ где-то менял подборку (`test_budget_answer_changes_the_selection`), эконом вёл местом по своему потолку, премиум поднимал дорогие впечатления, а ни один ответ не оставлял экран пустым (≥3 мест на каждый интерес) | `[x]` |
| L6 | Локаль, доки и версия: причина «Премиум-впечатление» в `tools/i18n_en/tr_ui.py` и пересборка `static/i18n/en.json`, правило и пример в [docs/data.md](docs/data.md#цена-и-уровень-из-одной-строки), `price` в примерах [docs/api.md](docs/api.md#get-apiattractions), README, CHANGELOG, версия 1.11.0 в `config.py`, `sw.js` и доках | `[x]` |

> Цена — не украшение карточки, а то, чем проверяется вопрос квиза.
> Раньше уровень ставил редактор, и проверить его было нечем: 42 места
> в «экономе» с билетами по 600 ₽ выглядели нормально. Теперь число
> выводится из текста подсказки тестом — расхождение видно сразу.

## Фаза 1.12.0 — «Описания и подписи» `[x]`

Цель: `description` места должны читать не только на карточке, а `description`
источников — видеть пользователю, а не редактору. Оба поля жили в данных
годами, и никто от них ничего не требовал. Ни одной выдуманной сущности:
описания растут из фактов той же записи, а поля источника просто доехали
до экрана.

| # | Задача | Статус |
|---|---|---|
| N1 | `description` всех 60 мест развёрнут в рассказ на 3–5 предложений (280–560 символов): первое — питч карточки (её обрезает `line-clamp: 3`), дальше — что это, что смотреть и практический нюанс. Цены и часы не переезжают в текст: у них `price_hint` и `hours` с машинными проверками | `[x]` |
| N2 | Правило читается из доков: `test_descriptions_are_fleshed_out` берёт разбег длины и минимум предложений из строки `description` в [docs/data.md](docs/data.md#описания--не-цитаты), требует уникальность по каталогу, запрещает «₽» и оборванные «…» — копия чисел в тесте разъехалась бы с документом (приём тот же, что у границ координат) | `[x]` |
| N3 | Английский каталог переписан по новым строкам (`tools/i18n_en/tr_places.py`), `en.json` пересобран; зеркалирование полей (`test_place_fields_mirror_the_russian_records`) не поехало | `[x]` |
| N4 | `GET /api/news`: `sources[]` отдаёт `description`, `url`, `type` — к `id`/`name`. Кроме первых двух поля опциональны: синтетический источник без подписи должен молчать, а не ронять ленту (деградация вместо падения) | `[x]` |
| N5 | Блок «📡 Об источниках» под лентой: вывеска, подпись, тип, ссылка и статус «не отвечает» для упавших источников — по уже существующему `failed_sources`, новых запросов фронт не делает | `[x]` |
| N6 | Контракт локали: `source_labels()` требует и вывески, и подписи источников — 11 переводов в `tr_ui.py`, `test_source_captions_are_translatable` закрепляет, что новая подпись в данных попросит перевод сама | `[x]` |
| N7 | Хвост аудита: опечатки каталога («мраморными дворикам и» → «двориками», «батарии» → «батареи»), README, CHANGELOG, версия 1.12.0 в `config.py`, `sw.js` и доках | `[x]` |

> Описание — единственное поле места, которое читают целиком, и единственное,
> от которого ничего не требовали. Теперь у него есть диапазон, структура
> и зеркало в английском словаре; числа же остаются там, где их может
> проверить машина.

## Бэклог (без привязки к версии)

План выполнен — открытых задач нет. Идеи ниже ждут повода, а не очереди:

- **Первый настоящий выкат по тегу**: workflow и `deploy.sh` проверены
  репетицией, CI и тестами, но по SSH не гонялись — нужен сервер и секреты
  (`DEPLOY_HOST`/`USER`/`PATH`/`SSH_KEY`, см. [deploy.md](docs/deploy.md#что-положить-в-секреты-github)).
- **Счётчики лимита на несколько воркеров**: сейчас бюджет живёт в памяти
  одного процесса, поэтому `uvicorn --workers 2` удваивает его. Повод —
  рост нагрузки; цена — внешний счётчик, которого принцип «без БД» не любит.
- **Третья речь**: ещё один `static/i18n/<lang>.json` плюс пункт
  в переключателе; покрытие нового языка обеспечит `tools/i18n_extract.py`.
- ~~**Подписи источников**: `description` в `sources.json` нигде не показывается
  (в ленте видна только вывеска — она переведена), поэтому и перевода у него
  нет. Появится экран «об источниках» — понадобятся и подписи, а контракт
  локали уже умеет их требовать (`source_labels()`).~~ Закрыто в 1.12.0
  «Описания и подписи»: блок «Об источниках» показывает подписи, а перевод
  требует `source_labels()`.
- **Перевод ленты «по-настоящему»**: машинного перевода нет и не будет;
  если появится спрос на англоязычные заголовки, это отдельный источник
  данных (англоязычные ленты Крыма), а не перевод чужих текстов.

Пункты ниже добавил аудит контента в 1.10.0 — они найдены в данных, но
без внешнего источника или живого сервера их не закрыть честно
(третий, про «премиум», закрыт в 1.11.0: уровни вывели из цен):

- **Расписания для остальных 41 места**: `hours`/`schedule` есть у 19.
  Пока графика нет, «открыто сейчас» такие места не прячет (честно), но и
  сезон у них — оценка редактора, а не факт. Нужен источник: сайты музеев
  и заповедников, а не догадка.
- **Фотографии мест**: у 60 карточек нет своего изображения — поле `img`
  пустое, все делят 8 картинок категорий (`static/img/cat_*.jpg`). Реальные
  фото упираются и в авторские права, и в бюджет веса (`BUDGET_TOTAL`
  1.3 МБ закреплён тестом): нужен либо свой фотобанк, либо ленивая
  загрузка отдельных карточек вне бюджета оболочки.
- ~~**«Премиум» почти ни на что не влияет**~~: закрыто в 1.11.0 «Цена
  вопроса» — уровни выведены из реальных цен, у вопроса появился смысл.

---

## Как вести этот файл

- Завершил пункт — отметь `[x]`, укажи коммит/PR.
- Новое предложение — сначала в «Бэклог», в фазу — только при старте фазы.
- Закрыл фазу — подними версию по semver, обнови `CHANGELOG.md`,
  сверься с чек-листом релиза в [docs/development.md](docs/development.md#релиз).
