# 🗺 План развития «Крым.Гид»

> Создан 16.09.2026. Текущая версия — **1.2.0**.
> Принципы из [docs/architecture.md](docs/architecture.md) сохраняются:
> без БД, без API-ключей, деградация вместо падения, фронт без сборки.

## Легенда статусов

`[x]` — готово · `[~]` — в работе · `[ ]` — запланировано

---

## Фаза 0.11.0 — «Качество и контракт API» `[x]`

Цель: жёсткий API-контракт, автоматический контроль стиля и версий,
первые JS-тесты. Без изменения пользовательского поведения.

| # | Задача | Статус |
|---|---|---|
| A1 | Валидация `QuizIn` через `Literal` → `422` на мусор вместо молчаливых дефолтов | `[x]` |
| A2 | `GET /api/weather?city=<неизвестно>` → `404` вместо «все города» | `[x]` |
| A3 | Тест согласованности версий: `config.py` == `sw.js` == `README.md` == `docs/api.md` | `[x]` |
| A4 | Ruff `check` в CI, `line-length = 88` по [dev-гиду](docs/development.md) | `[x]` |
| A5 | JS-тесты шаринга плана: вынести `buildPlanString`/`parsePlanParam` в `static/plan-link.js`, тесты на `node:test` без зависимостей + шаг в CI | `[x]` |
| A6 | `CHANGELOG.md`, бамп `0.11.0`, доки (`api.md`: `422`/`404`) | `[x]` |

## Фаза 0.12.0 — «Фронт и доступность» `[x]`

Цель: доступность, шаринг состояния, безопасность заголовков.

| # | Задача | Статус |
|---|---|---|
| B1 | a11y: осмысленные `alt` у изображений карточек, `focus trap` в модалке, `aria-live` у тостов, skip-link «к содержимому» | `[x]` |
| B2 | Фильтры каталога в URL (`#/catalog?tag=&area=&q=&sort=`) — шаринг подборок: `static/catalog-link.js` + 8 JS-тестов, кнопка «🔗 Ссылка на подборку» | `[x]` |
| B3 | Open Graph / Twitter Card мета-теги для шаринга ссылок (origin из `SITE_ORIGIN`/заголовков в `GET /`) | `[x]` |
| B4 | `Content-Security-Policy`: inline-обработчики убраны, PWA и Яндекс-ссылки работают; `frame-ancestors` настраивается env | `[x]` |
| B5 | Аудит веса картинок (`static/img` 3.1 МБ → 0.75 МБ): ресайз/качество через `tools/optimize_images.py`, `hero.jpg` в `fetchpriority="high"`, бюджет закреплён тестами | `[x]` |
| B6 | Тесты и доки: `tests/test_assets.py`, CSP/OG-тесты в `test_api.py`, CHANGELOG, версия 0.12.0 | `[x]` |

## Фаза 1.0 — «Контент и релиз» `[x]`

Цель: полнота данных и готовность к продакшену.

- [x] Аудит покрытия тегов: `TAGS` (15) vs опции квиза (15) vs `PROFILES` (15)
      vs `PURPOSE_TOPICS` (15) — все теги доступны в квизе, имеют профиль и
      маппинг на темы новостей; покрытие закреплено тестами.
- [x] Расширение каталога: 52 → **60 точек**, Западный Крым 8 → **16**
      (Сасык-Сиваш, Бакальская коса, Донузлав, Беляус, Межводное, кенасы,
      Окунёвка, «Крым в миниатюре»); поле `hours` у 13 объектов со
      стабильным расписанием. Минимум 10 мест на район — в тестах.
- [x] Деплой-документация: [docs/deploy.md](docs/deploy.md),
      `docker-compose.yml` (app + Caddy, HTTPS по ACME), `.env.example`,
      backup `cache/` — `deploy/backup-cache.sh` и `restore-cache.sh`;
      прод-конфигурация закреплена тестами и шагом CI.
- [x] E2E smoke ключевых сценариев (квиз → план → ссылка) —
      `tests/test_e2e_smoke.py`, 13 тестов. Playwright сознательно не взяли:
      он тянет npm и браузерные бинарники против принципа «фронт без
      сборки, тесты без сети». Сценарии закрыты на уровне HTTP плюс
      сверка Python-двойника ссылки с настоящим `plan-link.js` в Node.

## Фаза 1.1 — «После релиза» `[x]`

Цель: то, что стало видно на полном каталоге и живом деплое.

- [x] Ленивый рендер каталога: первая порция 12 карточек вместо 60,
      догрузка по прокрутке (`IntersectionObserver`) и кнопкой
      «Показать ещё» — `static/catalog-page.js`, 7 JS-тестов.
      Попутно починена повторная привязка обработчиков в `bindCards`.
- [x] `hours` для оставшихся объектов с расписанием: Мангуп-Кале,
      Эски-Кермен, Неаполь Скифский, Судакская крепость, Калос-Лимен,
      аквариум Алушты (13 → 19).
- [x] Фильтр «открыто сейчас» поверх `hours`: вместо парсинга свободного
      текста завели парное машинное поле `schedule` — текст остаётся
      точным для человека, структура фильтруется машиной, расхождение
      ловится тестом. `static/open-now.js`, 11 JS-тестов.
- [ ] Автодеплой из CI по тегу (`docker compose pull && up -d` по SSH) —
      перенесён в бэклог: нужен доступ к живому серверу и секреты
      в GitHub, чего в этой сессии нет.

> Точность «открыто сейчас» доведена до дня в 1.2 (`firstDay`/`lastDay`);
> в тексте `hours` остались только то, что закодировать нельзя —
> санитарные дни и закрытия на непогоду. Об этом интерфейс честно
> предупреждает: «уточняйте на месте».

## Фаза 1.2.0 — «Точность данных и шаринг избранного» `[x]`

Цель: убрать известную неточность «открыто сейчас» (огрубление до
месяца, оговорённую в 1.1) и сделать избранное шарящимся — как
подборка каталога.

| # | Задача | Статус |
|---|---|---|
| C1 | Дневная точность `schedule`: `firstDay`/`lastDay` в правилах `open-now.js`; точные границы у Ливадии, Массандры, Воронцовского дворца; подпись «открывается 16 июня» вне сезона; двусторонняя сверка границ с текстом `hours` тестом | `[x]` |
| C2 | Шаринг избранного: `static/favs-link.js` (UMD) + 9 JS-тестов; кнопка «🔗 Ссылка на избранное»; импорт по `#/favs?f=…` (только id из каталога получателя, только добавление) | `[x]` |
| C3 | `ruff format` для всего репо отдельным коммитом (17 файлов) + `format --check` в CI; версия ruff закреплена в `requirements-dev.txt` | `[x]` |

## Бэклог (без привязки к версии)

- Автодеплой из CI по тегу (`docker compose pull && up -d` по SSH):
  нужен живой сервер и секреты в GitHub.
- Почасовой прогноз и «купальный индекс» (температура воды Open-Meteo Marine).
- Leaflet-слой поверх схемы-карты (опция при наличии сети).
- Английская локаль ключевых экранов.
- Rate limiting на `/api/quiz/evaluate` и `/api/news?refresh=1`.

---

## Как вести этот файл

- Завершил пункт — отметь `[x]`, укажи коммит/PR.
- Новое предложение — сначала в «Бэклог», в фазу — только при старте фазы.
- Закрыл фазу — подними версию по semver, обнови `CHANGELOG.md`,
  сверься с чек-листом релиза в [docs/development.md](docs/development.md#релиз).
