# Схемы данных

Весь контент приложения — JSON-файлы в `app/data/`. Они читаются через
`app/services/load.py` с `lru_cache` (файл читается один раз за процесс).
Ниже — контракты, которым данные обязаны соответствовать; большинство
проверяется тестами (`tests/test_data.py`).

## attractions.json

Каталог мест. Массив объектов:

| Поле | Тип | Обязательно | Ограничения | Описание |
|---|---|---|---|---|
| `id` | string | ✔ | уникальный, slug (`a-z`, `-`), без пробелов | ключ для URL `#/place/<id>`, избранного |
| `name` | string | ✔ | | название места |
| `type` | string | ✔ | ключ `TYPE_META` | тип точки (см. [справочники](#справочники)) |
| `region` | string | ✔ | ≥3 символов | локация для подзаголовка («Ялта → Ай-Петри») |
| `area` | string | ✔ | ключ `AREA_LABEL` | географический район (кластер планировщика) |
| `description` | string | ✔ | 280–560 символов, ≥3 предложений | рассказ о месте: первое предложение — питч карточки, дальше — для модалки (см. [правило](#описания--не-цитаты)) |
| `tags` | string[] | ✔ | ⊆ `TAGS`, непусто | теги матчера |
| `season` | string[] | ✔ | ⊆ `{spring, summer, autumn, winter}`; у мест с `schedule` — ровно те сезоны, в которые место работает | когда стоит ехать |
| `budget` | int | ✔ | 1–3 | 1 — до 1 000 ₽, 2 — до 3 000 ₽, 3 — дороже; полоса **самой дорогой** цены из `price_hint` (см. [правило](#цена-и-уровень-из-одной-строки)) |
| `duration_h` | number | ✔ | 0 < x ≤ 12 | сколько часов закладывать; ≥6 — «на целый день» |
| `rating` | number | ✔ | 0–5 | популярность (лёгкий бонус в балле) |
| `price` | int | ✔ | ≥0 | минимальная цена, за которую место доступно (₽ за человека): 0 — вход свободный |
| `price_hint` | string | ✔ | содержит `₽` или «бесплатно» | «вход ~300 ₽», «бесплатно» — текст для человека |
| `hours` | string | | 5–200 символов, содержит время `9:00` | часы работы — **только там, где график стабилен** |
| `schedule` | object[] | | нужен ⟺ есть `hours` | машинная проекция `hours` для фильтра «открыто сейчас» |
| `tips` | string | ✔ | | практический совет для модалки |
| `access` | string | ✔ | `car` `transit` `both` | доступность без машины |
| `lat` | float | ✔ | 44.3–45.9 | широта (точка на схеме-карте) |
| `lng` | float | ✔ | 32.3–36.7 | долгота |

Границы — с запасом вокруг реального каталога: самая западная точка
Тарханкут (≈32.49 в. д.), самая северная — Бакальская коса (≈45.75 с. ш.).
`tests/test_data.py::test_coordinate_bounds_in_docs_cover_the_catalog`
не даёт им «протухнуть»: точка за пределами заявленного диапазона —
либо ошибка в координатах, либо таблицу нужно расширять осознанно.

### Цена и уровень из одной строки

`price_hint` — это текст для человека («билет ~500 ₽, скала и вид —
бесплатно»), и выводить из него уровень бюджета «на глаз» оказалось
нельзя: 42 места из 60 стояли в «экономе», включая билеты по 600 ₽,
а уровень 3 был ровно один — сафари-парк «Тайган». Ответ «премиум»
в квизе почти ничего не менял.

Теперь у подсказки две машинные проекции, как `schedule` у `hours`:

- `price` — **минимум**, за который место доступно: 0, если в подсказке
  есть бесплатный вариант, иначе самая низкая цена из неё. По `price`
  эконом понимает, за вход в какое место он заплатит больше своего
  потолка (`1 000 ₽`), — «дорого» звучит только там, где это правда.
- `budget` — **полоса самой дорогой** цены из подсказки: ≤1 000 ₽ → 1,
  ≤3 000 ₽ → 2, дороже → 3. Это «сколько место может вместить»: у мыса
  с бесплатным входом и дайвингом за 4 000 ₽ уровень 3, и премиуму
  он интереснее, чем эконому. Диапазон «300–500 ₽» даёт и 300, и 500.

Пороги — те же числа, что в подсказках квиза («до 1 000 ₽»,
«1 000–3 000 ₽»); `BUDGET_CEILING` в `recommend.py` и текст
`quiz.json` сверяет `test_budget_thresholds_match_the_quiz`.

Правило держат тесты в обе стороны: `test_price_is_the_lowest_price_of_the_place`
и `test_budget_level_follows_the_highest_price` читают подсказку и требуют,
чтобы число совпадало. Поменяли цену в тексте, не тронув `price` или
`budget`, — тест покажет место. Ещё один следит, что шкала не вырождается:
`test_budget_levels_use_the_whole_scale` (у уровня 3 есть адресаты,
а потолок эконома что-то отсекает).

Минимальный пример:

```json
{
  "id": "new-place",
  "name": "Новое место",
  "type": "nature",
  "region": "Судак, окрестности",
  "area": "Восточный",
  "description": "Короткое описание для карточки.",
  "tags": ["nature", "photo"],
  "season": ["spring", "autumn"],
  "budget": 1,
  "duration_h": 3,
  "rating": 4.5,
  "price": 0,
  "price_hint": "бесплатно",
  "tips": "Вода и удобная обувь обязательны.",
  "access": "car",
  "lat": 44.95,
  "lng": 35.1
}
```

### Поле `hours`

Единственное необязательное поле каталога. Заполняется у музеев,
дворцов, пещер и парков — там, где расписание публикует сам объект
и оно не меняется от недели к неделе. У пляжей, мысов и природных
точек его быть не должно: **отсутствие поля честнее выдуманного
графика**. Сезонные различия пишем в одну строку:

```json
"hours": "9:00–18:00 (апрель–октябрь), 9:00–17:00 (ноябрь–март), касса за час до закрытия"
```

Во фронтенде строка показывается в модалке места отдельной широкой
строкой блока «факты»; если поля нет, строка не рендерится.

### Поле `schedule`

Машинная проекция `hours`: по ней работает фильтр «🕘 Открыто сейчас»
(`static/open-now.js`). Поля обязаны идти парой — текст без структуры
не фильтруется, структура без текста лишает человека точных оговорок
(санитарные дни, «касса за час до закрытия»); это проверяет тест.

```json
"schedule": [
  { "months": "4-10", "from": "09:00", "to": "18:00" },
  { "months": "11-3", "from": "09:00", "to": "17:00", "closed": ["mon"] }
]
```

| Ключ | Формат | Смысл |
|---|---|---|
| `months` | `"4-10"` | диапазон месяцев; `"11-3"` — через Новый год |
| `from` / `to` | `"09:00"` | окно работы, `from < to` |
| `closed` | `["mon"]` | выходные дни: `mon`…`sun` |
| `firstDay` | `16` | опционально: окно стартует с этого числа **старт-месяца** («16 июня» у правила `"6-10"`) |
| `lastDay` | `15` | опционально: окно заканчивается этим числом **завершающего** месяца («15 октября» у правила `"6-10"`) |

Пример с дневными границами (Ливадийский дворец: «10:00–19:00
(16 июня–15 октября), 10:00–18:00 (май–15 июня), 10:00–17:00
(ноябрь–апрель)»):

```json
"schedule": [
  { "months": "5-6", "lastDay": 15, "from": "10:00", "to": "18:00" },
  { "months": "6-10", "firstDay": 16, "lastDay": 15, "from": "10:00", "to": "19:00" },
  { "months": "10-4", "firstDay": 16, "from": "10:00", "to": "17:00" }
]
```

Ограничения (все закреплены тестами `test_data.py`):

- месяцы покрыты целиком — дыра означала бы «закрыто навсегда»,
  а это почти всегда забытое правило;
- каждое время из `schedule` встречается в тексте `hours` — иначе
  бейдж «открыто» противоречил бы карточке;
- дневные границы `schedule` (`firstDay`/`lastDay`) и датировки в
  `hours` («16 июня», «15 сентября») обязаны совпадать в обе стороны;
  допущена одна неявная граница — день сразу после конца соседнего
  окна в том же месяце («…15 октября» + зимнее окно с 16-го);
- первое подходящее правило выигрывает, поэтому узкие диапазоны
  ставьте выше широких — и в месяцах (`"7-8"` выше `"4-10"`),
  и в днях.

**Точность — до дня.** Сезонные границы задаются `firstDay`/`lastDay`;
когда дата не попадает ни в одно окно, подпись показывает точную дату
открытия («Закрыто · открывается 16 июня»). То, что закодировать
нельзя (санитарные дни, закрытия на непогоду), остаётся в тексте
`hours` — поэтому интерфейс показывает дисклеймер «уточняйте на месте»,
а время считается по Крыму (UTC+3) независимо от пояса устройства.

### Описания — не цитаты

До 1.12.0 `description` был однострочным питчем: карточка с ним жила,
а в модалке читать было нечего. Теперь это рассказ на 3–5 предложений
(280–560 символов), и правило к нему такое же, как к цене и расписанию:
текст обязан читаться из фактов той же записи, а не из фантазии автора.

- первое предложение — питч: его видно на карточке (CSS `line-clamp: 3`
  обрезает остальное, длина сетку не ломает);
- дальше — чем место интересно, что там смотреть и один практический
  нюанс («утром тише», «тени нет»);
- цены и часы в описание не переезжают: у них свои `price_hint` и
  `hours`, тест запрещает «₽» в `description` — иначе два поля однажды
  разойдутся, и непонятно, кому верить;
- `test_descriptions_are_fleshed_out` держит разбег длины, число
  предложений и уникальность: дубль «интересное место для всех» —
  тоже способ ничего не рассказать.

Английская версия описания живёт в `tools/i18n_en/tr_places.py` и
обязана зеркалить набор полей записи (`test_places_cover_the_catalogue`,
`test_place_fields_mirror_the_russian_records`) — изменили русский
текст, перевод правим в том же заходе.

После добавления объекта точка сама появится в API, каталоге, квизе
и на схеме-карте. Прогоните `pytest tests/test_data.py` — он проверит
все ограничения из таблицы выше.

## quiz.json

Контент квиза: объект с `intro` и массивом `questions` (ровно 7).

| Поле | Тип | Ограничения | Описание |
|---|---|---|---|
| `id` | string | см. ниже | `purpose` `season` `tempo` `budget` `party` `duration` `transport` |
| `type` | string | `multi` / `single` | мультивыбор только у `purpose` |
| `max` | int | только у `multi` | максимум выбранных вариантов |
| `emoji` | string | | иконка вопроса |
| `title` / `subtitle` | string | | текст вопроса / подсказка |
| `options[]` | object[] | id уникальны вопросе | варианты ответа |

Опция: `{ "id": "beach", "emoji": "🏖️", "label": "Море и пляжи", "hint": "…" }`
(`hint` необязателен).

Согласованность с кодом (проверяют тесты):

- опции `purpose` ⊆ ключей `TAGS`;
- опции `duration` ⊆ ключей `DAYS_BY_DURATION` и `ITINERARY_DAYS`;
- каждый тег `TAGS` доступен как опция первого вопроса `purpose`;
- профиль отдыха `PROFILES[tag]` существует для любого выбранного интереса;
- связь интереса с темами ленты `PURPOSE_TOPICS[tag]` задана для каждого тега.

## sources.json

Источники новостей. Массив объектов:

| Поле | Тип | Ограничения | Описание |
|---|---|---|---|
| `id` | string | уникальный | попадёт в `items[].source_id` |
| `name` | string | | показывается в `items[].source` и `failed_sources` |
| `url` | string | http(s), уникальный | RSS/XML-лента или `https://t.me/s/<канал>` |
| `type` | string | `rss` / `telegram` | выбирает парсер |
| `description` | string | | подпись источника: видна в блоке «Об источниках» под лентой (1.12.0) и обязана иметь перевод — контракт держит `source_labels()` |

Для `telegram` важно: работают только **публичные** каналы (превью
`t.me/s/…`, последние ~20 постов, без медиа). Недоступные источники не
роняют ленту — попадают в `failed_sources`.

## news_snapshot.json

Офлайн-снапшот: показывается, когда недоступны все источники.

```json
{
  "fetched_at": "2026-09-15T12:00:00+03:00",
  "items": [
    {
      "title": "…", "link": "https://…", "source": "ТАСС",
      "published": "2026-09-15T…", "summary": "…", "sample": true
    }
  ]
}
```

Требуется: непустой `items`; у каждого элемента — `title`, `link`,
`source`. Флаг `sample: true` помечает демонстрационные материалы
(фронтенд показывает дисклеймер). Темы (`topics`) снапшоту досчитывает
сервер при выдаче.

## Формат элемента новости (API)

То, что получает фронт после `_normalize()`:

| Поле | Тип | Описание |
|---|---|---|
| `id` | string | `n<N>` — живая лента, `s<N>` — снапшот |
| `title` | string | ≤200 символов, без HTML |
| `link` | string | URL материала |
| `summary` | string | ≤300 символов, без HTML |
| `published` | string | ISO 8601 или исходная строка даты |
| `source` / `source_id` | string | имя и id источника |
| `crimea_score` | int | 0 — не про Крым; чем больше, тем «крымче» |
| `topics` | string[] | до 3 тем |

## Справочники

**Теги (`TAGS`, 15 шт.)**: `beach` пляжи · `nature` природа ·
`history` история · `culture` дворцы и музеи · `food` гастрономия ·
`wine` вино · `active` активный отдых · `extreme` экстрим и дайвинг ·
`family` с детьми · `photo` фотогении · `spa` спа и грязели ·
`city` набережные и города · `view` панорамы · `free` бесплатно ·
`romance` для двоих. Все 15 тегов доступны в первом вопросе квиза `purpose`,
имеют профиль в `PROFILES` и маппинг на темы новостей в `PURPOSE_TOPICS`.

**Типы (`TYPE_META`)**: `beach` Пляж · `castle` Крепость · `palace` Дворец ·
`winery` Вина · `nature` Природа · `park` Парк · `city` Город ·
`spa` Спа · `food` Гастро · `museum` Музей · `active` Активный ·
`factory` Детям. У каждого — `emoji`, `label`, `img` (файл в `static/img/`,
существование проверяет тест).

**Районы (`AREA_LABEL`)**: `Южный берег` (17) · `Западный` (16) ·
`Центральный` (15) · `Восточный` (12) — всего **60 точек**. Тест
`test_catalog_size_and_area_coverage` требует минимум 10 мест в каждом
районе: планировщик кластеризует день по району, и на «тонком» районе
он не собрал бы полноценный день.

**Слоты плана**: `morning` утро · `afternoon` день · `evening` вечер ·
`full` целый день.

**Темы новостей**: `safety` ✋ безопасность · `transport` транспорт ·
`beach` пляжи · `weather` погода · `nature` природа · `events` события ·
`food` гастро · `history` история.
